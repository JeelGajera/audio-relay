# CLAUDE.md

See [`AGENTS.md`](AGENTS.md) — that's the canonical, tool-agnostic guide for
working in this repository (build/test commands, conventions, what needs
real hardware to verify). This file exists only because Claude Code looks
for `CLAUDE.md` specifically; keep it a pointer rather than a fork, so the
two never drift out of sync.
